# Cập nhật đăng nhập, tài khoản sinh viên và tự kết nối

Từ bản này, cập nhật trực tiếp các file trong dự án hiện có. Chỉ tạo lại ZIP khi bạn yêu cầu rõ ràng. ZIP ngày 14/09 được giữ nguyên; các thay đổi ngày 16/09 nằm trong mã nguồn và JAR ở thư mục dự án này.

## Chỉ cần cập nhật 4 file mã nguồn

Nếu đang dùng bản trong thư mục này, mã và JAR đã được cập nhật. Nếu đang dùng một thư mục giải nén hoặc kho GitHub khác, sao chép đúng 4 file sau vào cùng vị trí trong dự án của bạn:

| Thao tác | File trong cấu trúc dự án | Nội dung |
|---|---|---|
| Thay file | [src/App.java](src/App.java) | Khởi động ứng dụng, gọi màn hình đăng nhập mới và tự kiểm tra kết nối. |
| Thêm file mới | [src/ui/LoginPanel.java](src/ui/LoginPanel.java) | Toàn bộ giao diện đăng nhập mới, hai nút chọn vai trò, trạng thái kết nối và menu Cài đặt. |
| Thay file | [src/service/AccountService.java](src/service/AccountService.java) | Thêm `provisionMissingStudents(...)` để cấp tài khoản còn thiếu, giữ nguyên mật khẩu/trạng thái của tài khoản đã có. |
| Thay file | [src/ui/AccountAdminPanel.java](src/ui/AccountAdminPanel.java) | Số lượng đã/chưa cấp, nút cấp hàng loạt, tên đăng nhập và trạng thái đúng với CSDL. |

Sau khi chép file: đóng ứng dụng → chạy **BUILD.bat** → mở **RUN.bat**. Nếu chỉ thay `.java` mà mở JAR cũ thì giao diện vẫn là bản cũ.

Muốn tự chỉnh tiếp giao diện: màu và font ở đầu `LoginPanel.java`; bố cục form trong hàm khởi tạo; phần trang trí trái trong `BrandPanel`; tự kết nối ở `connectAutomatically()`; menu cấu hình ở `showSettings()`.

## Vì sao trước đó sinh viên chưa đăng nhập được?

Khi kiểm tra, CSDL có **100 sinh viên, 0 tài khoản sinh viên và 1 quản trị viên**. Bảng `SINH_VIEN` lưu hồ sơ; bảng `QLTC_TAI_KHOAN` mới lưu tài khoản đăng nhập. Có hồ sơ chưa đồng nghĩa đã có mật khẩu.

Sau khi cập nhật:

1. Chọn **Cán bộ đào tạo** và đăng nhập bằng tài khoản quản trị đã tạo.
2. Vào **Tài khoản & Ngoại ngữ**.
3. Bấm **Cấp tài khoản còn thiếu**, đặt và nhập lại mật khẩu tạm từ 8–128 ký tự.
4. Chờ thông báo hoàn tất. Mỗi tài khoản dùng **mã sinh viên làm tên đăng nhập**; mật khẩu tạm là mật khẩu bạn vừa đặt.
5. Sinh viên chọn vai trò **Sinh viên**, đăng nhập và đổi mật khẩu lần đầu.

Nút cấp hàng loạt áp dụng cho tất cả sinh viên chưa có tài khoản trong danh sách đã tải, kể cả dòng bị ẩn bởi bộ lọc. Chạy lại sẽ bỏ qua tài khoản đã có. Để đổi riêng một tài khoản, chọn dòng rồi bấm **Cấp / Đặt lại mật khẩu**.

## SQL kết nối tự động như thế nào?

Ứng dụng tự đọc cấu hình đã lưu rồi kiểm tra kết nối ở nền mỗi lần mở. Khi thành công, cuối form hiện **Dữ liệu đã kết nối**. Màn hình không tự bật hộp cấu hình; nếu kết nối lỗi, chọn **Cài đặt → Cấu hình kết nối dữ liệu** hoặc **Thử lại**.

Thông tin máy chủ/tài khoản chỉ cần điền một lần trong `config/database.properties` của bản đang dùng. Ví dụ cấu trúc:

```properties
db.url=jdbc:sqlserver://localhost:1433;databaseName=QuanLyTinChi_NangCap_20260914;encrypt=true;trustServerCertificate=true
db.user=sa
db.password=MAT_KHAU_SQL_SERVER_CUA_BAN
```

Thay giá trị cuối bằng mật khẩu SQL Server thực tế; không dùng nguyên chữ mẫu. Nếu chưa có file, dùng màn hình Cài đặt để lưu hoặc sao chép từ `config/database.properties.example`. `.gitignore` tiếp tục bỏ qua cấu hình có mật khẩu riêng.

Trên máy mới cần có SQL Server và dữ liệu đã khôi phục trước; ứng dụng không tự suy đoán mật khẩu SQL Server. Nếu CSDL chưa có quản trị viên, khi kết nối thành công menu Cài đặt sẽ hiện **Tạo quản trị viên đầu tiên**.

## Kiểm tra đã thực hiện

- Biên dịch thành công bằng JDK 21, nhắm Java 17.
- 34 kiểm tra logic và 12 kiểm tra cấu hình đạt.
- 14 kiểm tra cấp tài khoản đạt: phân quyền, đăng nhập bằng tài khoản mới, yêu cầu đổi mật khẩu, bỏ qua tài khoản cũ, giữ trạng thái khóa, salt riêng, hoàn tác khi SQL lỗi và dọn dữ liệu kiểm thử.
- Kiểm tra kết nối khởi động thực tế: có cấu hình hợp lệ → READY; thiếu mật khẩu → OFFLINE và hiển thị hướng dẫn ngay trong form.
- Kiểm tra hình ảnh đăng nhập ở 1280×820, 1000×700, trạng thái chưa kết nối và bảng tài khoản.

Kiểm thử giao diện bằng dựng các thành phần Swing và kiểm tra ảnh, không phải thử mọi thao tác chuột trên cửa sổ thật. Dữ liệu/tài khoản kiểm thử đã được dọn; việc cấp mật khẩu cho 100 sinh viên thực hiện bằng nút trên với mật khẩu bạn chọn.

File kiểm thử bổ sung: `tests/AccountProvisionTests.java` (không cần sao chép để chạy ứng dụng).

![Giao diện đăng nhập mới](preview/19_dang_nhap_20260916.png)
